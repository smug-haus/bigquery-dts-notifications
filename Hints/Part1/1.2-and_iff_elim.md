---
exercise: Part1/1.2/and_iff_elim
module: "1.2"
---

## Level 1 — Nudge

Package the pair, apply `.mp`.

## Level 2 — Sketch

- After `intro h p q`, you have `h : P ∧ Q ↔ R`, `p : P`, `q : Q`.
- `h.mp` goes from `P ∧ Q` to `R`.
- Build the pair from `p` and `q`; apply.

## Level 3 — Skeleton

```lean
intro h p q
exact sorry
```
