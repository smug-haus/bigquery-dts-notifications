---
exercise: Part1/1.1/union_compl_univ
module: "1.1"
---

## Level 1 — Nudge

The universal set is trivially true for every input.

## Level 2 — Sketch

- `univ a` unfolds to `True`.
- You do not actually need to use the hypothesis `a ∈ union s (compl s)`.
- `trivial` or `exact True.intro` finishes.

## Level 3 — Skeleton

```lean
intro a _
sorry   -- close a `True` goal
```
