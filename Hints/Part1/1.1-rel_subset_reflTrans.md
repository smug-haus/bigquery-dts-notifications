---
exercise: Part1/1.1/rel_subset_reflTrans
module: "1.1"
---

## Level 1 — Nudge

A one-step path is `refl` followed by `tail`.

## Level 2 — Sketch

- `ReflTransClosure` has two constructors: `refl` and `tail`.
- `tail` takes an already-built closure and one more `r`-step.
- Starting from `ReflTransClosure.refl a`, apply `tail` once with the
  hypothesis.

## Level 3 — Skeleton

```lean
intro a b hab
exact ReflTransClosure.tail sorry sorry
```
