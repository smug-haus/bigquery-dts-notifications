---
exercise: Part1/1.2/or_assoc_fwd
module: "1.2"
---

## Level 1 — Nudge

Nested case split — three leaves.

## Level 2 — Sketch

- `cases` the outer `∨`.
- In the left branch, the hypothesis is itself `P ∨ Q`; `cases` again.
- You end up with three leaves: from `P`, from `Q`, from `R`. Commit
  each to the correct branch of the right-associated goal.

## Level 3 — Skeleton

```lean
intro h
cases h with
| inl hpq =>
  cases hpq with
  | inl hp => sorry
  | inr hq => sorry
| inr hr => sorry
```
