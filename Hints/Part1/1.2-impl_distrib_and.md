---
exercise: Part1/1.2/impl_distrib_and
module: "1.2"
---

## Level 1 — Nudge

Project out of the conjunction at each use site.

## Level 2 — Sketch

- Given `h : P → Q ∧ R`, you need to produce two functions:
  one `P → Q`, one `P → R`.
- For each, feed `p` through `h` and then project with `.1` or `.2`.

## Level 3 — Skeleton

```lean
intro h
exact ⟨fun p => sorry, fun p => sorry⟩
```
