---
exercise: Part1/1.2/and_assoc_fwd
module: "1.2"
---

## Level 1 — Nudge

Destructure then repack.

## Level 2 — Sketch

- The hypothesis `(P ∧ Q) ∧ R` unfolds to a nested pair.
- Pattern-match with `intro ⟨⟨hp, hq⟩, hr⟩`.
- The goal `P ∧ (Q ∧ R)` is built with `⟨hp, hq, hr⟩` (Lean
  flattens anonymous-constructor pairs automatically).

## Level 3 — Skeleton

```lean
intro ⟨⟨hp, hq⟩, hr⟩
exact sorry
```
