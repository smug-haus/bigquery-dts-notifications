---
exercise: Part1/1.1/inter_comm
module: "1.1"
---

## Level 1 — Nudge

Membership in `inter s t` is a pair. Swap it.

## Level 2 — Sketch

- A proof of `a ∈ inter s t` unfolds to `a ∈ s ∧ a ∈ t`.
- You want `a ∈ inter t s`, which is `a ∈ t ∧ a ∈ s`.
- Destructure the incoming pair and repackage it.

## Level 3 — Skeleton

```lean
intro a ⟨hs, ht⟩
exact ⟨sorry, sorry⟩
```
