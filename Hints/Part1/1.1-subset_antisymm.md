---
exercise: Part1/1.1/subset_antisymm
module: "1.1"
---

## Level 1 — Nudge

An iff is two implications.

## Level 2 — Sketch

- Goal: `∀ a, a ∈ s ↔ a ∈ t`.
- Introduce `a`.
- An iff splits into two directions; each direction is a hypothesis
  you already have (`hst` and `hts`) specialized at `a`.

## Level 3 — Skeleton

```lean
intro a
exact ⟨sorry, sorry⟩   -- one hypothesis for each direction
```
